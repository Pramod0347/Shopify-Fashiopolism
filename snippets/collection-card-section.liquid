<div class="collection-list--image mb-10">
  <!--
    <a href="{{ collection.url }}">
    {% render 'image',
      class: 'media media--portrait collection_img',
      mobileSrc: collection.image,
      desktopSrc: collection.image
    %}
    </a>
  -->
  <a href="{{ collection.url }}">
    <img
      src="{{ collection.image | image_url: width: 1500 }}"
      loading="lazy"
      width="{{ collection.image.width }}"
      height="{{ collection.image.width | divided_by: collection.image.aspect_ratio | round }}"
    >
  </a>
</div>
<div class="collection-list--title center">
  <div>
    {{ collection.title }}
  </div>
  <div>{{ collection.all_products_count }} items</div>
</div>
